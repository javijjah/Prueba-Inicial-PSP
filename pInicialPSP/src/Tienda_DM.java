import java.util.HashSet;

public class Tienda_DM {
    //con más tiempo habría desarrollado esta clase y sus métodos, haciendo los métodos del menú en base a
    //estas colecciones y restando unidades en stock al comprar discos o películas.
    public HashSet<Disco> discos;
    public HashSet<Pelicula> peliculas;
    public HashSet<Cliente_registrado> clientes;
}
